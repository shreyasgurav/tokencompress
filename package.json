{
  "name": "tokencompress",
  "version": "0.6.1",
  "description": "Removes redundant tokens from LLM inputs before they hit the model. Shows exactly what was dropped and why.",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "tokencompress": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc -p tsconfig.json --noEmit",
    "validate": "vitest run tests/validation/",
    "benchmark": "npx tsx benchmarks/squad.ts",
    "benchmark:quick": "npx tsx benchmarks/squad.ts --limit 20",
    "benchmark:long": "npx tsx benchmarks/longdoc.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "llm",
    "compression",
    "tokens",
    "context",
    "ai",
    "tiktoken",
    "prompt",
    "optimization",
    "explainable"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "js-tiktoken": "^1.0.15"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "node-fetch": "^3.3.2",
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
